// const fs = require('fs');

// const filePath = 'TherapyAvatar/src/data/scenarios.ts';
// let content = fs.readFileSync(filePath, 'utf8');

// // Replace options: ["string", "string"] with options: [{ text: "string" }, { text: "string" }]
// content = content.replace(/options: \["([^"]*)", "([^"]*)"\]/g, 'options: [{ text: "$1" }, { text: "$2" }]');

// fs.writeFileSync(filePath, content);
// console.log('Fixed all options.');
